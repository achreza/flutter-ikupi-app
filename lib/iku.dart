import 'package:flutter/material.dart';

class ikuScreen extends StatelessWidget {
  const ikuScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("IKU"),
      ),
    );
  }
}
