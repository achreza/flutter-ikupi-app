import 'package:flutter/material.dart';

class listKendalaScreen extends StatelessWidget {
  const listKendalaScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("LIST KENDALA"),
      ),
    );
  }
}
